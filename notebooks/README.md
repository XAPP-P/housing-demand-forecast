# Notebooks

Research notebooks from earlier modules — kept for reference and reproducibility.
The dashboard itself does not import from these.

- `module1_*.ipynb` — EDA and baseline modeling (Module 1)
- `module2_*.ipynb` — ZIP-level prices, division-level sales, construction base model (Module 2)
- `module2_map_prototype.ipynb` — the TIGER + Zillow join prototype that seeded the Price Map tab
